public class InfiniteLoopsDemo {
    public static void main(String[] args) {

        int x = 0;
        int y = 5;

        //  -Примеры бесконечного while-

        // Пример 1 — классический
        /*
        while (true) {
            // бесконечный цикл
        }
        */

        // Пример 2 — условие всегда истинно
        /*
        while (x == 0) {
            // бесконечный цикл
        }
        */


        // - Примеры бесконечного do-while -

        // Пример 1 — классический
        /*
        do {
            // бесконечный цикл
        } while (true);
        */

        // Пример 2 — условие всегда истинно
        /*
        do {
            // бесконечный цикл
        } while (y == 5);
        */

        System.out.println("Программа завершена (все циклы закомментированы, если раскомментировать любой — он станет бесконечным).");
    }
}
